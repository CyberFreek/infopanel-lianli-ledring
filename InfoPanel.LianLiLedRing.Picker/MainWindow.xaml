<mah:MetroWindow x:Class="InfoPanel.LianLiLedRing.Picker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
        Title="Lian Li LED Ring - Colors"
        Width="440"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        TitleCharacterCasing="Normal">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Colors used count  -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Colors used"
                       VerticalAlignment="Center"
                       FontSize="13"
                       Margin="0,0,10,0" />
            <mah:NumericUpDown x:Name="CountBox"
                               Width="90"
                               Minimum="1"
                               Maximum="6"
                               Value="6"
                               Interval="1"
                               ValueChanged="CountBox_ValueChanged" />
            <TextBlock Text="  (multi-color effects cycle through these)"
                       VerticalAlignment="Center"
                       FontSize="11"
                       Opacity="0.6" />
        </StackPanel>

        <!--  Six color rows  -->
        <ItemsControl Grid.Row="1" x:Name="ColorList">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="70" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Text="{Binding Label}"
                                   VerticalAlignment="Center"
                                   FontSize="13"
                                   Opacity="{Binding Opacity}" />
                        <metro:ColorPicker Grid.Column="1"
                                           Height="34"
                                           Opacity="{Binding Opacity}"
                                           metro:TextBoxHelper.UseFloatingWatermark="False"
                                           SelectedColor="{Binding Color, Mode=TwoWay}" />
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <TextBlock Grid.Row="2"
                   Margin="0,10,0,0"
                   FontSize="11"
                   Opacity="0.6"
                   TextWrapping="Wrap"
                   Text="Changes apply to the ring immediately. Close this window when done." />

        <!--  Buttons  -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Reset to Rainbow" Width="130" Margin="0,0,10,0" Click="Reset_Click" />
            <Button Content="Close" Width="90" IsDefault="True" Click="Close_Click" />
        </StackPanel>
    </Grid>
</mah:MetroWindow>
